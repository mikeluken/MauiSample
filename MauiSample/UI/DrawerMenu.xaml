<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="MauiSample.UI.DrawerMenu"
             BackgroundColor="#dcdcdc"
             Title="My App"
             >
    <ContentPage.Resources>
        <ResourceDictionary>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <StackLayout x:Name="layoutContainer" BackgroundColor="#dcdcdc" Padding="0,0,0,0" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <StackLayout Orientation="Horizontal" VerticalOptions="Center" HorizontalOptions="FillAndExpand" Padding="20" BackgroundColor="#343434" x:Name="profileWrapper">

                <Button
                    Text="LOGOUT"
                    Clicked="Button_Clicked" />

            </StackLayout>
        </StackLayout>
    </ScrollView>
</ContentPage>